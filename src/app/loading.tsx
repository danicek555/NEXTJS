import React from "react";

//loading file will show when we are loading the website
const Loading = () => {
  return (
    <div>
      <h1 className="text-3xl">AH</h1>
    </div>
  );
};

export default Loading;
